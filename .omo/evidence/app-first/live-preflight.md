# Live prerequisite and secondary-workspace preflight (read-only)

- Observation window: `2026-09-06T16:05:21Z` to `2026-09-06T16:07:20Z` (UTC).
- Transport: Windows native `C:\Windows\System32\OpenSSH\ssh.exe`, alias `jarvis-server`, with `BatchMode=yes`, `ConnectTimeout=8`, and `StrictHostKeyChecking=yes`.
- Scope: read-only live inspection only. No time, account, ACL, firewall, package, Tailscale, firmware, power, WOL, repository, or application mutation was issued.
- Source control: no Git history, remote URL, secret/auth-store, private-key, password, token, or `.env` content was read.

## T8: current live prerequisites

| Scenario | Invocation (all via the native SSH options above) | Binary observable | Result |
| --- | --- | --- | --- |
| Authenticated current access | `id -un; hostname; date -u '+UTC=%Y-%m-%dT%H:%M:%SZ'` | SSH completed; remote identity and clock emitted | **PASS**: connected as `armanc` to `armanc`; remote clock was `2026-09-06T16:05:21Z`. |
| Time synchronization current state | `timedatectl show -p TimeUSec -p NTPSynchronized -p NTPService -p CanNTP -p LocalRTC --no-pager; systemctl is-active systemd-timesyncd.service chrony.service ntp.service; chronyc tracking; chronyc sources -v` | `chrony.service=active`, `Leap status=Normal`, current NTP source selected | **PASS (chrony)**: `systemd-timesyncd=inactive`, `chrony=active`, `ntp=inactive`; `timedatectl` reported `NTPSynchronized=no`, but chrony reported stratum 3, normal leap status, and `17 ns slow`. The distinction is material: the timedatectl flag does not demonstrate a failing active chrony synchronizer. |
| Historical clock/TLS cause and correction | `journalctl -u chrony.service -n 40 --no-pager` filtered to time/sync/error lines; `journalctl -u tailscaled.service -n 120 --no-pager` filtered to clock/time/x509/state/error lines, with HTTP(S) URLs redacted | Logs show prior certificate-not-yet-valid failures, then an exact clock step and subsequent daemon recovery | **PASS, correction already happened without this task**: chrony logged `System clock wrong by 3519322.832640 seconds` on Jul 27 and `System clock was stepped by 3519322.832640 seconds` at `2026-09-06 15:39:34 UTC`. Prior chrony and Tailscale TLS errors were consistent with a clock before certificate validity. Tailscale logged `time jump detected (slept 977h35m38s)`, then `NoState -> Starting -> Running` and `health(...login-state): ok`. |
| Tailscale current health | `systemctl show tailscaled.service -p ActiveState -p SubState -p StateChangeTimestamp --no-pager; tailscale status; tailscale status --json` filtered to `BackendState`, `Online`, `TailscaleIPs`, `Health` | `tailscaled` running; JSON has `BackendState: Running`, `Health: []`, and online peers | **PASS (current state)**: daemon `active/running`; JSON backend `Running`, health empty, and online entries. Text status showed both `jarvis-server` (`100.105.31.70`) and `armanc-windows` (`100.87.132.67`). Earlier logged logout/NoState was historical, not current. |
| Timers after large clock step | `systemctl list-timers --all --no-pager --no-legend; systemctl show fwupd-refresh.timer -p ActiveState -p SubState -p NextElapseUSecRealtime -p LastTriggerUSec --no-pager` | Active waiting timer and future elapse recorded; timer last triggers show step time | **PASS with watch gate**: `fwupd-refresh.timer` was `active/waiting`, last triggered `2026-09-06 15:39:34 UTC`, next at `16:56:18 UTC`. Other scheduled timers remained listed with future runs. `ua-timer`, `update-notifier-download`, and `systemd-tmpfiles-clean` retain Jul 27 last-trigger timestamps, so post-step behavior must be rechecked after their next scheduled runs; no claim of a full timer catch-up audit is made. |
| `fwupd-refresh` prior failure reevaluation | `systemctl show fwupd-refresh.service -p LoadState -p ActiveState -p SubState -p Result -p ExecMainStatus -p ExecMainCode -p StateChangeTimestamp --no-pager; journalctl -u fwupd-refresh.service -n 80 --no-pager` with URLs redacted | Old failure followed by successful refreshes; current unit result success | **PASS (refresh only; no firmware update)**: Jul 27 had `status=1/FAILURE`. The observed Sep 6 trigger and multiple later refreshes logged successful LVFS metadata download; current unit was `inactive/dead`, `Result=success`, `ExecMainStatus=0`. It reports 10 updatable devices / 3 published supported updates, but no upgrade was run or authorized. |
| Failed services and JARVIS health | `systemctl --failed --no-legend --no-pager; systemctl show jarvis-health.service jarvis-agent.service ...; systemctl list-unit-files 'jarvis*'; systemctl list-units --all 'jarvis*'; systemctl show jarvis-host-telemetry.service ...` | Empty failed-services output; named health/agent absent; telemetry active | **PASS/UNKNOWN split**: no failed unit was listed. `jarvis-health.service` and `jarvis-agent.service` are `not-found`, so those names cannot be used as health proof. `jarvis-host-telemetry.service` is enabled and `active/running` with `Result=success`; it runs as root. Functional health of its workload is **unknown** because no service payload/log content was inspected. |
| Read-only helper and elevation boundary | `sudo -n /usr/local/sbin/jarvis-remediation --version; --check; --effective-config; --network-intel; sudo -n true; sudo -n -l` | Helper check emits `JARVIS_REMEDIATION_READY`; general passwordless sudo denied; only explicit helper commands listed | **PASS for existing read-only helper; BLOCKED for general elevation**: `--check` succeeded and effective SSH configuration reports port 22, root login disabled, public key auth enabled, password auth disabled, X11 disabled. General `sudo -n true` returned `interactive authentication is required`. The only relevant NOPASSWD commands are the existing helper checks/status/effective-config/listeners/network-intel (plus pre-existing mutating entries that were not invoked). No noninteractive, authorized time-correction mechanism was found or executed. |
| UFW/Fail2Ban authorization | Existing `sudo -n true` and `sudo -n -l` boundary; helper `--network-intel` | General sudo requires interaction; helper reports `ufw=ACTIVE` and listener classifications | **UNKNOWN for direct UFW/Fail2Ban inspection**: direct privileged UFW/Fail2Ban queries were intentionally not attempted because they are not in the NOPASSWD allowlist. `ufw=ACTIVE` is helper output only; it must not be treated as a LAN-only proof. Fail2Ban status was not authorized/observed. |
| Helper network output reliability | `sudo -n /usr/local/sbin/jarvis-remediation --network-intel` | Output includes `awk` warning while still producing listener rows | **NOT READY as sole security evidence**: helper output identifies exposed SSH 22 (IPv4/IPv6), HTTP 80, XRDP 3389, and 9090, but emitted `awk: ... regexp escape sequence ... warning` and its listener labels are legacy heuristics. Do not use it as verified source-aware authorization evidence until T5 fixes/validates the JSON producer. |

### T8 gates

1. **Time gate currently satisfied by chrony, not by this task.** Before any deployment, repeat `date -u`, `chronyc tracking`, and the Tailscale JSON health check in the same authenticated session. Do not manually set time: current chrony synchronization is healthy and a prior automatic step corrected the fault.
2. **Tailscale gate currently satisfied, but owner authentication remains a recovery gate.** If `BackendState` or health regresses, use the owner's direct interactive Tailscale authentication; do not run `tailscale up/login/logout` unattended.
3. **Elevation gate blocks live helper/sudoers deployment.** The authenticated session cannot obtain general noninteractive sudo. A human must provide direct interactive elevation at deployment time, after local package/helper gates and a fresh backup; no credential replay is permitted.
4. **Firewall/Fail2Ban gate remains unverified.** A human-approved privileged read-only check is required before claiming retained LAN access or Fail2Ban state. A denied query is not evidence of a LAN-only firewall.
5. **Timer watch gate.** Reinspect `ua-timer`, `update-notifier-download`, and `systemd-tmpfiles-clean` after their next planned runs; the clock step reset the observed timeline but did not prove every timer's task outcome.

## T9: secondary JARVIS workspace and least-privilege inventory

| Scenario | Invocation | Binary observable | Result |
| --- | --- | --- | --- |
| Candidate workspace inventory | For `/home/armanc/JARVIS`, `/home/armanc/JARVIS_WORKSPACE`, `/srv/...`, `/opt/...`: `test -d`, `ls -ld`, `du -sh`, `git -C ... rev-parse --is-inside-work-tree` | Presence, size, and Git-worktree predicate | **PASS**: `/home/armanc/JARVIS` exists (4.0K) and `/home/armanc/JARVIS_WORKSPACE` exists (240K); neither is a Git worktree. `/srv` and `/opt` candidates are absent. |
| Nested repository/dirty-state guard | `find ... -type d -name .git -prune -printf '%h\n'`; then `git status --short --branch`, `git diff --quiet`, and `git diff --cached --quiet` for each result | `NESTED_GIT=NONE` | **PASS**: no nested Git repository was found below the two designated workspace roots, so there is no repository worktree/branch/dirty state to preserve in the inspected scope. No remote URL or history was queried. |
| Designated workspace ACL and agent identity | `getfacl -cp` on both roots; `id jarvis-agent` | ACL text and agent group list | **PASS (current least privilege)**: `/home/armanc/JARVIS_WORKSPACE` grants `user:jarvis-agent:r-x` only; `jarvis-agent` is only in its own group (not sudo/docker). `/home/armanc/JARVIS` has normal owner/group rwx and other r-x, with no named agent ACL. No ACL change was made. |
| Service and GitHub-auth availability | `systemctl --user show jarvis-health.service jarvis-agent.service ...; command -v gh; gh auth status` only when present | Units not found; CLI availability result | **UNKNOWN**: user units `jarvis-health`/`jarvis-agent` are not found, and `gh` is absent, so existing GitHub account/repository authentication could not be verified. No new login, credential, or account change was attempted. |
| Broader privilege risk observation | `id; groups; sudo -n -l` | `armanc` is in sudo and docker groups; NOPASSWD list constrained to helper commands | **WATCH**: `armanc` retains sudo and Docker-group membership. This pre-existing access was not altered. The separate `jarvis-agent` identity does not have those groups, and its designated workspace access is read/execute only. |

### T9 gates

1. Keep the current `jarvis-agent` read/execute-only ACL on `JARVIS_WORKSPACE` unless the owner explicitly authorizes a task-specific access design. It currently has neither sudo nor Docker-group membership.
2. Do not infer repository health or GitHub authorization from directory presence: both designated roots have no Git worktree, and the GitHub CLI is absent. Owner input is required if a specific repository/account must be targeted.
3. `jarvis-host-telemetry.service` is root-run and outside the `jarvis-agent` least-privilege boundary; altering it would be a separate, owner-authorized live change.

## Cleanup receipt and explicit unknowns

- No task-owned remote processes, files, services, credentials, tunnels, or temporary resources were created; nothing requires remote cleanup.
- One attempted PowerShell construction for the workspace query failed locally with a parser error before SSH invocation; it created no remote state. The corrected read-only query then completed with SSH exit `0`.
- The local `omo ulw-loop status --json` command returned `The syntax of the command is incorrect`; no `currentAttemptDir` could be obtained. This report was therefore placed in the plan's existing evidence root.
- Unknown means unknown: direct UFW/Fail2Ban state, full timer execution after the step, telemetry workload health, and GitHub account/repository authorization remain unverified. None is represented as a passing live-deployment condition.

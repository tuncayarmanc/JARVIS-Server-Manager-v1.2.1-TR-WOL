# jarvis-security-v1-2-2 - Work Plan

> STOPPED BY USER — 2026-09-06. This proposed implementation plan is retained for a future explicit resumption. Final independent approval is incomplete. Do not start its tasks automatically; the current planning operation is closed. The stop notice supersedes execution language below.

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** J.A.R.V.I.S v1.2.2 will report the server's security state accurately, recognize Cockpit and XRDP, keep its file manager inside the intended home area, use a single restricted maintenance helper, and install as a verified Turkish Windows release.

**Why this approach:** The historical conversation contained stale and contradictory claims, so the plan starts from the current v1.2.1 source and live Ubuntu evidence. Server changes are transactional and every success message requires an observable postcondition.

**What it will NOT do:** It will not open SSH to the Internet, broaden passwordless root access, alter the public web ports, run system updates or power actions, create a GitHub repository, or modify/use the remote-open/Wake-on-LAN feature.

**Effort:** Large
**Risk:** High - the release includes a root-owned helper and a live server deployment, although disruptive operations are explicitly excluded.
**Decisions I made for you:** I treated source-restricted listeners as LAN/tailnet reachability rather than Internet exposure; kept the human administrator's normal password-protected sudo; retained only exact helper actions for the app; selected per-user NSIS as the supported Windows upgrade path; documented unsigned packages; left remote-open/WOL untouched as requested.

Your next move: none now; the user requested stopping after the current planning operation. The following tasks are a retained proposal, not an active execution queue.

---

> TL;DR (machine): Large/high-risk direct-work hardening across helper, parser, file-manager, Turkish UI, postcondition state machines, live non-disruptive deployment, and v1.2.2 NSIS/MSI release; remote-open/WOL excluded.

## Scope
### Must have
- Preserve the verified SSH policy (`PasswordAuthentication no`, `PubkeyAuthentication yes`, `PermitRootLogin no`, `MaxAuthTries 3`, `X11Forwarding no`) and both LAN and Tailscale access.
- Make `helper.sh` the canonical 1.2.2 helper source and generate/verify the self-contained installer copy so the historical single-file SCP workflow remains valid.
- Remove the live AWK warning and emit deterministic listener records that distinguish local, LAN-reachable, tailnet, host-public-allowed, protected, and unknown states without claiming WAN reachability.
- Recognize `cockpit.socket`/9090 and `xrdp.service`/3389; preserve UFW, Nginx, CUPS, Cockpit, XRDP, and ports 80/443 unchanged during source/deployment work.
- Restrict helper argv/arity, sanitize its environment, make helper+sudoers replacement atomic, retain bounded immutable SSH backup IDs, and verify effective state after apply/rollback.
- Treat remote-path checks as a file-manager safety guardrail: reject traversal through symlink components for read/write/upload/download while allowing list visibility and link-object rename/delete without following the target.
- Keep update/reboot/poweroff behind explicit confirmations and fixed helper actions; show an update preview; report success only from action-specific postconditions.
- Translate every user-visible v1.2.2 surface to Turkish while normalizing internal TypeScript identifiers to ASCII English names, except exact identifiers inside Task 1's byte-frozen WOL/direct-dependency slices; record those exceptions explicitly rather than editing the deferred feature.
- Synchronize 1.2.2 across npm, Cargo, Tauri, helper and README version surfaces; build fresh NSIS/MSI artifacts, publish checksums, perform a per-user NSIS upgrade, and capture fresh connected UI evidence.

### Must NOT have (guardrails, anti-slop, scope boundaries)
- Never put a private key, sudo password, token, cookie, environment dump, or raw auth log in source, evidence, commands, or the ledger.
- Never open TCP/22 to the Internet, broaden Tauri to arbitrary local shell executables, or grant NOPASSWD for bash/apt/systemctl/arbitrary helper arguments.
- Do not remove the human administrator's ordinary password-protected sudo access; `sudo -n true` must continue to fail.
- Do not alter Nginx or UFW rules for 80/443, disable services, run OS upgrades, reboot, or power off without a fresh safety-critical approval and physical/console recovery path.
- Do not modify, invoke, or test the remote-open/Wake-on-LAN feature in this release; the user explicitly deferred it.
- Do not claim Internet exposure from bind address or LAN reachability alone; label the observable host firewall/source scope.
- Do not claim race-proof sandboxing against the authenticated SSH user; the application intentionally exposes that user's full terminal.
- Do not initialize or push a Git/GitHub repository, purchase/provision signing identity, edit generated `dist`/`target`/`.bak` files, or add a new permanent test framework.
- Do not promise package-update rollback; `--update` changes installed packages and is outside live QA for this plan.

## Verification strategy
> Agent-executed verification with one explicit human authorization gate: at Task 7 the user types the sudo credential directly into a visible SSH PTY. Agents never receive or replay it. All other checks are agent-executed.
- Test decision: add narrow reproducible tests for the security boundaries without adding a test framework: shell golden/transaction scripts, Rust unit tests, and Node's built-in test runner for extracted TypeScript state logic. Use baseline-before-change commands, compiler/build gates, and real desktop/server QA; remove temporary processes/files and record cleanup.
- Evidence root: `.omo/evidence/`; each task writes the exact artifact names stated in its QA scenarios.
- Source gates: `npm run build`; `cargo check --all-targets --all-features` and `cargo test --all-targets --all-features` with fresh task-specific target dirs; `bash -n helper.sh`; `bash -n server-setup/install-jarvis-remediation.sh`; `bash scripts/tests/test-helper-transaction.sh`; `bash scripts/tests/test-network-intel.sh`; `node --test` against the compiled TypeScript state tests; `node scripts/sync-helper.mjs --check`.
- Security gates: no legacy AWK warning in live `--network-intel`; exact known rows for SSH, XRDP and Cockpit; malformed helper args denied; `sudo -n true` denied; before/after hashes for UFW/Nginx/SSH effective policy unchanged unless the task explicitly owns the file.
- Desktop gate: installed v1.2.2 process path/version/registry, direct UI screenshot, `BAĞLI`, Ubuntu statistics, Security Center rows, update preview, and safe cancel paths for destructive actions.
- Release gate: NSIS per-user upgrade succeeds; MSI all-users requirement is documented rather than treated as corruption; Authenticode unsigned status disclosed; SHA-256 manifest covers final MSI/NSIS/EXE.

## Execution strategy
### Durable execution state and evidence ledger
- Before Task 1, the orchestrator creates `.omo/boulder.json` for plan/task status and append-only `.omo/start-work/ledger.jsonl`; Task 1 validates both and becomes the baseline entry. Only the orchestrator appends records after independently confirming returned artifacts; workers and reviewers do not edit the ledger.
- Each ledger line is canonical compact JSON with fixed key order: `seq`, `timestamp_utc`, `task`, `phase`, `artifact`, `sha256`, `command_summary`, `exit_code`, `agent_id`, `verdict`, `plan_sha256`, `previous_record_sha256`, `record_sha256`. Secrets/raw auth/environment are forbidden. `record_sha256` hashes the UTF-8 canonical record excluding that key; `previous_record_sha256` chains to the prior line. Sequence starts at 1 and increments by one.
- Every task/reviewer artifact gets a PASS/FAIL/APPROVE/REJECT record with its current SHA; missing or changed artifacts invalidate later claims. F1 reads the sealed Tasks 1-8 prefix. F1-F4 each receive their own ledger record; after all approve, root writes `.omo/evidence/final/ledger-final.sha256` and no further append is allowed unless the seal is explicitly invalidated and final verification reruns.

### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.
- Wave 1: Task 1 captures immutable baseline/rollback evidence.
- Wave 2: Tasks 2 and 4 run in parallel because helper/installer files and Rust file-manager code do not overlap.
- Wave 3: Task 3 builds and tests the producer plus Rust consumer after Tasks 2 and 4, avoiding concurrent edits to `src-tauri/src/lib.rs`.
- Wave 4: Tasks 5 then 6 serialize because both own `src/main.ts` and operational state depends on the typed UI model.
- Wave 5: Task 7 stages the verified helper/installer delta, pauses for the user's direct PTY authorization, then deploys and runs non-disruptive live checks.
- Wave 6: Task 8 versions, packages, upgrades, and manually QA's the desktop artifact.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | none | 2,3,4,5,6,7,8 | none |
| 2 | 1 | 3,6,7 | 4 |
| 3 | 2,4 | 5,7 | none |
| 4 | 1 | 3,8 | 2 |
| 5 | 3 | 6,8 | none (owns `src/main.ts`) |
| 6 | 2,5 | 8 | none (owns `src/main.ts`) |
| 7 | 2,3 | 8 | none (live helper deployment) |
| 8 | 4,5,6,7 | final wave | none |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [ ] 1. Capture the authoritative v1.2.1 baseline and full rollback package
  What to do / Must NOT do: stop any stale JARVIS process; validate `.omo/boulder.json` and the empty/initial append-only ledger schema; inventory source excluding `node_modules`, `dist`, `.codegraph`, `src-tauri/target*`, `.install-rollback`, `.omo/evidence`, screenshots and logs; archive every owned source/config/helper file plus installed v1.2.1 NSIS setup, `%LOCALAPPDATA%\J.A.R.V.I.S Server Manager`, and `%LOCALAPPDATA%\com.armanc.jarvis-server-manager`; write SHA-256, owner/mode/registry/process/server-state manifests with sensitive values redacted. Produce a user-visible string inventory with explicit protocol/product/WOL exceptions. Create `.omo/evidence/check-wol-boundary.ps1`, `task-1-wol-boundary.json`, and `task-1-wol-identifier-exceptions.txt`; every hash entry records file, unique start/end regex anchors, occurrence, exact captured bytes, and SHA-256, while the exception file lists exact identifiers and containing hash entries (including `ConnectionDurum`, `AppDurum`, `sshBaşlating`, and `waitForServerDurum` where captured). Freeze Rust `validate_host`, `validate_port`, `parse_mac_address`, `wake_on_lan`, `probe_server`, and handler registration; TypeScript WOL state/default/settings/UI/event slices, `setConnectionStatus`, `connectTerminal`, `disconnectTerminal`, `setPowerButton`, `waitForServerDurum`, and exact offline WOL branch from its unique confirmation anchor through its catch. Task 6 adds separate action-state helpers instead of editing these frozen dependencies. Record live helper hash/version, SSH policy, network-intel stderr/stdout, `sudo -n -l`, Tailscale, health, UFW-derived listener output, Nginx hashes, installed-app registry, and preflight both identities: full helper reachability as `armanc@192.168.1.10`, plus BatchMode identity/connectivity and expected `sudo -n true` denial as `jarvis-agent@100.105.31.70`. Must NOT initialize Git, read private-key content, run privileged mutation, or overwrite the 20260904 rollback.
  Parallelization: Wave 1 | Blocked by: none | Blocks: 2,3,4,5,6,7,8
  References (executor has NO interview context - be exhaustive): `.install-rollback/20260904-203848`; `.active-rollback-path`; `findings.md`; `progress.md`; installed app `C:\Users\arman\AppData\Local\J.A.R.V.I.S Server Manager`; server `/usr/local/sbin/jarvis-remediation`, `/usr/local/bin/jarvis-health`, `/home/armanc/JARVIS_WORKSPACE/backups`.
  Acceptance criteria (agent-executable): boulder identifies this exact plan SHA and ledger line 1 validates its canonical hash chain; a timestamped `.install-rollback/v1.2.2-prechange-*` archive, `.omo/evidence/task-1-baseline.json`, `task-1-localization-inventory.txt`, `task-1-wol-boundary.json`, and exact identifier-exception file exist; every included file verifies; excluded/generated/secret paths are absent; restoring to a temporary directory reproduces every archived hash; WOL extractor reproduces all region hashes; BatchMode SSH succeeds as `armanc` over LAN; BatchMode SSH succeeds as `jarvis-agent` over Tailscale, reports that identity, and captured `sudo -n true` status is nonzero. If either preflight fails, record it before implementation and remove the failed route from Task 7 acceptance rather than attributing it to deployment.
  QA scenarios (name the exact tool + invocation): happy: PowerShell `Expand-Archive` the archive into a new temp directory and `Get-FileHash` every manifest entry; failure: verify a deliberately altered temporary copy fails checksum comparison while the original archive remains unchanged. Evidence `.omo/evidence/task-1-baseline.json` and `task-1-restore-check.txt`.
  Commit: N | no Git repository; immutable rollback manifest is the provenance anchor.

- [ ] 2. Make helper 1.2.2 canonical, transactional, and exact-argument restricted
  What to do / Must NOT do: make root `helper.sh` the only editable helper source; update it to 1.2.2 with fixed `--update-preview`, `--update`, `--poweroff`, `--reboot`, read-only audit, remediation, and explicit rollback-ID actions; add exact arity checks, fixed PATH/environment, regular-file/no-symlink checks for managed SSH config, bounded retention of 10 root-owned backup directories while preserving the active ID. Add `scripts/sync-helper.mjs` to generate/check the installer heredoc so the installer remains one SCP-able file, and `scripts/start-supervised-deploy.ps1` to prepare a 16-lowercase-hex session ID, installer SHA, unprivileged SCP path, and the fixed PTY commands without accepting any password parameter. Make installer replacement transactional, not falsely two-file atomic: validate staged helper and sudoers first, record prior absence/bytes/owner/mode, install the helper by same-directory atomic rename before the sudoers file, validate the final pair after each per-file rename, and use `EXIT/HUP/INT/TERM` traps to restore both files (including prior absence) until an explicit commit flag. Give `server-setup/install-jarvis-remediation.sh` a named `--supervised-deploy <session-id> <installer-sha256> <timeout-seconds>` mode. Session ID must match `^[a-f0-9]{16}$`, SHA `^[a-f0-9]{64}$`, timeout 300-1800 seconds (Task 7 uses 900). After verifying its root-owned staged self, it arms rollback and prints exact five-field TSV receipts `JARVIS_DEPLOY\t2\t<session-id>\t<ROLLBACK_ARMED|COMMITTED|ROLLED_BACK|ERROR>\t<deadline-epoch|evidence-sha256|reason-code>`. It waits on the same PTY for exact `COMMIT <session-id> <evidence-sha256>` or `ABORT <session-id>`, rejects non-TTY input and all other tokens, rolls back on EOF/HUP/INT/TERM/EXIT/timeout, and exits 0 only after COMMITTED, 20 for pre-mutation validation error, 30 for explicit/check rollback, 31 for timeout, or 32 for EOF/signal rollback. Use a sudoers anchored argument regex for rollback and exact fixed commands; remove `autoremove`/`autoclean` from update. Structure the transaction functions so a checked-in shell harness can source them without running `main` and instrument each replacement/validation/commit boundary. Must NOT add generic command forwarding or change SSH/UFW/service state during installation.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 3,6,7
  References (executor has NO interview context - be exhaustive): `helper.sh:1-310`; `server-setup/install-jarvis-remediation.sh:17-369`; `src/main.ts:1062-1081,1146-1156,2163-2188`; `src-tauri/src/lib.rs:1516-1599`; sudoers regex guidance in `findings.md` resources.
  Acceptance criteria (agent-executable): helper and embedded heredoc bytes match after generation; `--version` is 1.2.2; fixed actions reject extra args; rollback accepts only `/var/lib/jarvis-server-manager/jarvis-ssh-[A-Za-z0-9]{6}`; LF-normalized candidate sudoers passes exact remote nonprivileged `visudo` check below. Transaction matrix covers prior states absent/absent, present/absent, absent/present, present/present; for each it proves one successful COMMIT preserves new bytes/metadata and exact `ROLLBACK_ARMED` then `COMMITTED` receipts/exit 0, and every failpoint (after helper, after sudoers, after final validation, immediately before commit) under normal error plus EXIT/HUP/INT/TERM restores exact prior bytes, presence, owner, group, and mode with exact receipt sequence/exit code. Install instrumentation proves production requests regular non-symlink `root:root` destinations, helper `0755`, sudoers `0440`, while Task 7 proves live metadata; supervisor tests cover wrong SHA/session/token, no TTY, EOF, timeout, ABORT, valid COMMIT and specified receipts/exits; update preview uses `apt-get -s upgrade`; update contains only `apt-get update` and `apt-get upgrade -y` with lock timeout.
  QA scenarios (name the exact tool + invocation): happy: `& 'C:\Program Files\Git\bin\bash.exe' -n helper.sh`, same for installer, `node scripts/sync-helper.mjs --check`, then `& 'C:\Program Files\Git\bin\bash.exe' scripts/tests/test-helper-transaction.sh`; create a sanitized candidate sudoers fixture and run `& 'C:\Program Files\Git\bin\bash.exe' -lc "tr -d '\r' < .omo/evidence/candidate-jarvis-sudoers | ssh -o BatchMode=yes -o ConnectTimeout=8 armanc@192.168.1.10 '/usr/sbin/visudo -cf -'"`, requiring exit 0 without sudo. Failure: the transaction matrix exercises all four failpoints, all four prior-state combinations, and EXIT/HUP/INT/TERM; separately change one embedded byte in a temporary installer copy and require `--check` nonzero without rewriting it. Evidence `.omo/evidence/task-2-helper.txt`, `task-2-transaction-matrix.txt`, `task-2-visudo.txt`, and `task-2-sync-failure.txt`.
  Commit: N | no Git repository; task-1 rollback plus task evidence records the delta.

- [ ] 3. Replace heuristic network parsing with a deterministic host-firewall scope protocol
  What to do / Must NOT do: remove invalid AWK quote escape; parse exact listener endpoint/process/PID and resolve socket units from unit column; recognize Cockpit by `cockpit.socket`/9090 and XRDP by process/unit/3389. Emit header `JARVIS_NETWORK_INTEL\t2\t<OK|ERROR>\t<diagnostic>` then exactly 11-field records `LISTENER\t<family>\t<transport>\t<bind-address>\t<port>\t<process>\t<pid>\t<unit>\t<decision>\t<scope-set>\t<rule-source>`. Normal tokens: family `ipv4|ipv6`, transport `tcp|udp`, port 1-65535, numeric PID or `UNKNOWN`, unit ending `.service|.socket` or `UNKNOWN`; escape backslash/tab/CR/LF in free text. Emit one record per distinct `(family,transport,bind-address,port,pid,unit)` owner, deduplicate, sort family/transport/port/bind/PID/unit; UI aggregates owners after decoding. Loopback is `ALLOW/LOCAL/loopback`. A specifically bound non-loopback address evaluates only its containing connected subnet/interface; wildcard evaluates every domain in its family. Each connected subnet is its own domain, `TAILNET` for `tailscale0` or 100.64.0.0/10, otherwise `LAN`. Evaluate numbered UFW rules first-match for exact family/protocol/port/interface; a domain is ALLOW/DENY only if ordered rules/default give same result over entire CIDR, else UNKNOWN for partial overlap/unresolved interface. `rule-source` grammar is fixed `LAN=[<cidr>@<iface>=<ALLOW|DENY|UNKNOWN>@<rule-number|default|ambiguous>,...];TAILNET=[...];PUBLIC=[<ipv4|ipv6>=<ALLOW|DENY|UNKNOWN>@<rule-number|default|ambiguous>];DEFAULT=<ALLOW|DENY|UNKNOWN>@policy`; validate CIDR and interface token `[A-Za-z0-9_.:-]+`, sort subnet entries numerically by network then prefix then interface, and use empty `[]` for absent classes. Thus arbitrarily many LANs and conflicting interfaces retain distinct decisions. `scope-set` is ordered `LOCAL,LAN,TAILNET,HOST_PUBLIC_ALLOWED,PROTECTED,UNKNOWN`: include LAN/TAILNET if any domain in that class allows, HOST_PUBLIC_ALLOWED only for family-wide allow, PROTECTED if any applicable domain denies, UNKNOWN if any unresolved; mixed tags may coexist. Summary decision is ALLOW if any domain allows, otherwise UNKNOWN if any unknown, otherwise DENY for explicit deny, DEFAULT_ALLOW/DEFAULT_DENY only if default alone determines all applicable domains. Inactive UFW is UNKNOWN. Successful empty `ss` emits `OK/NONE`, no records, empty stderr, exit 0. Failed command/missing UFW/malformed row/invalid enum emits ERROR plus exact UNKNOWN sentinel, diagnostic stderr, exit 2; Rust rejects malformed counts/escapes into one typed UNKNOWN/error UI state. Never claim WAN reachability; exact-port matching must separate 22/2222 and 90/9090. Put Rust decoder/aggregator in focused tested module after Task 4.
  Parallelization: Wave 3 | Blocked by: 2,4 | Blocks: 5,7
  References (executor has NO interview context - be exhaustive): `helper.sh:116-236`; installer generated copy; `src-tauri/src/lib.rs:1295-1450`; live baseline in `progress.md` showing AWK warning, `*:3389 xrdp`, `*:9090 systemd`; verified units `xrdp.service`, `cockpit.socket` -> `cockpit.service`.
  Acceptance criteria (agent-executable): checked-in goldens cover IPv4/IPv6, wildcard/specific binds, multi-owner, multiple LAN subnets with conflicting interface decisions, simultaneous LAN+Tailscale, rule shadowing, partial CIDRs, interface rules, inactive/default policy, Cockpit/XRDP/unknown, escaping, 22/2222 and 90/9090; producer/Rust consumer agree exactly on schema, decoded subnet map, sorting, aggregation, exits/errors; successful-empty is `OK/NONE` zero records/empty stderr/exit 0, failed/malformed is ERROR+sentinel/stderr/exit 2; syntax/sync pass.
  QA scenarios (name the exact tool + invocation): happy: `& 'C:\Program Files\Git\bin\bash.exe' scripts/tests/test-network-intel.sh` diffs production parser output against checked-in goldens, then `$env:CARGO_TARGET_DIR='src-tauri/target-check-network-v122'; cargo test --manifest-path src-tauri/Cargo.toml network_intel`; failure: malformed `ss`, UFW, escape, and interface rows must produce a nonzero producer result or `ERROR`/`UNKNOWN`, visible diagnostic, and a Rust decode error where required. Evidence `.omo/evidence/task-3-network-fixtures.txt` and `task-3-malformed.txt`; retain the checked-in harness and sanitized fixtures for regression coverage.
  Commit: N | no Git repository; checksummed task evidence records parser behavior.

- [ ] 4. Enforce remote file-manager guardrails
  What to do / Must NOT do: in `src-tauri/src/lib.rs`, retain lexical normalization then add remote canonical-parent/existing-target checks under `/home/<username>`; reject intermediate symlink traversal for list/read/write/upload/download/create; allow listing a symlink and allow rename/delete only when operating on the link object without following it. For nonexistent targets canonicalize the existing parent. Use `--` before path operands and preserve existing size/name validation. Document the non-adversarial/TOCTOU limitation because the same user controls the SSH terminal. Must NOT claim chroot/openat2-grade confinement, restrict the interactive terminal, or modify/invoke/test WOL code.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 8
  References (executor has NO interview context - be exhaustive): `src-tauri/src/lib.rs:274-350,300-319,371-478`; `src/main.ts:1160-1360`; file-manager threat-model findings in `findings.md`.
  Acceptance criteria (agent-executable): Rust unit tests cover the path-decision matrix; `npm run build` and fresh-target `cargo check/test --all-targets --all-features` pass; source contains one reusable existing-target guard and one parent guard used by every named operation; delete refuses the home root; delete/rename use unlink/link-object semantics without following; every remote command places `--` before path operands; WOL boundary hashes equal Task 1.
  QA scenarios (name exact tool + invocation): create `scripts/qa-file-guard.ps1`; build/start with `$env:CARGO_TARGET_DIR='src-tauri/target-dev-task4-v122'; npm run tauri dev -- --no-watch`, wait for `src-tauri/target-dev-task4-v122/debug/jarvis-server-manager.exe`, then run `powershell -File scripts/qa-file-guard.ps1 -Executable src-tauri/target-dev-task4-v122/debug/jarvis-server-manager.exe -Host 192.168.1.10 -Port 22 -FixtureRoot /home/armanc/JARVIS_WORKSPACE/.qa-v122 -EvidenceDir .omo/evidence`. It creates a fixed tree with inside file/directory, leaf/intermediate/destination-parent symlinks and drives list/read/write/upload/download/mkdir/touch/rename/delete, requiring legitimate operations and link-object rename/delete to pass. Failure matrix rejects data operations through either symlink, home-root delete, and rename into symlinked destination parent with unchanged outside hashes. Cleanup only by explicit resolved unlink/file-by-file removal after exact prefix assertion; never recursive unresolved path. Evidence `.omo/evidence/task-4-file-guard.png`, `task-4-operation-matrix.txt`, `task-4-remote-hashes.txt`, cleanup receipt; close dev process.
  Commit: N | no Git repository; task-1 rollback plus evidence records the change.

- [ ] 5. Make Security Center exposure/state data accurate and finish Turkish UI normalization
  What to do / Must NOT do: update Rust/TypeScript listener models for the versioned protocol and render host-firewall source scope without using “İnternete açık” unless `HOST_PUBLIC_ALLOWED` is actually observed. Show Cockpit and XRDP names/units. Surface parser stderr/fallback source and distinguish `UNKNOWN` from zero. After security apply/rollback, rerun the effective audit; show `UYGULANDI`, `GERİ ALINDI`, or `DOĞRULANAMADI` only from postcondition evidence and clear stale modal state on refresh failure. Extract pure TypeScript security/action state mapping into a typed module and cover it with Node built-in tests for apply-helper-success/audit-failure and rollback-helper-success/audit-failure as well as normal success. Create `scripts/qa-installed-app.ps1` here, owning scenarios `SecurityCenter`, `ActionPrompts`, `Full`, and `Disconnected`, with stable visible-label/automation selectors, 30-second per-step timeouts, process cleanup, UI Automation assertions, and foreground `Graphics.CopyFromScreen` evidence capture. Translate user-visible English identified by Task 1 while renaming internal Unicode/Turkish identifiers to ASCII English names; explicitly exempt Task 1's byte-frozen WOL slices and shared direct dependencies, which remain as shipped. Preserve protocol/service/product constants and Turkish visible copy. Keep current CSP/shell capability unchanged and accurately document that remote command text remains arbitrary by design for the full SSH terminal.
  Parallelization: Wave 4 | Blocked by: 3 | Blocks: 6,8
  References (executor has NO interview context - be exhaustive): `src/main.ts:8-107,634-748,899-1021,1191-1468,1899-2251,2309-2470`; `src-tauri/src/lib.rs:1295-1599`; `src-tauri/capabilities/default.json`; `src-tauri/tauri.conf.json:20-31`; live connected screenshot `jarvis-v1.2.1-connected.png`.
  Acceptance criteria (agent-executable): `rg` finds no known corrupted identifier outside exact entries in `task-1-wol-identifier-exceptions.txt`, and no untranslated visible phrase outside WOL/protocol/product exceptions; `npm run build`, Node state tests, fresh-target Cargo check/test pass; parser failure renders `DOĞRULANAMADI` with unknown/error, simultaneous scopes render faithfully, 3389/9090 show XRDP/Cockpit and LAN not public; every WOL/direct-dependency hash equals Task 1.
  QA scenarios (name the exact tool + invocation): happy: run the desktop app and use `powershell -File scripts/qa-installed-app.ps1 -Scenario SecurityCenter -EvidenceDir .omo/evidence` to connect, open Güvenlik, and capture exact Cockpit/XRDP/source-scope labels. Failure: Node built-in tests feed `APPLY_HELPER_OK_AUDIT_ERROR` and `ROLLBACK_HELPER_OK_AUDIT_ERROR` and require the stale secure state to clear to `DOĞRULANAMADI`; start only a development build with `VITE_JARVIS_QA_FIXTURE=network-malformed` to render the parser failure visibly. Run the production Vite build without that variable and require `rg` to find neither fixture payload nor activation token in `dist`; the installed release has no activation path. Evidence `.omo/evidence/task-5-security-center.png`, `task-5-localization.txt`, `task-5-postcondition-tests.txt`, `task-5-parser-failure.png`, and `task-5-release-fixture-scan.txt`; close the development process.
  Commit: N | no Git repository; task evidence plus rollback manifest records the delta.

- [ ] 6. Add action-specific update, reboot, and shutdown postconditions
  What to do / Must NOT do: add a read-only update preview in the UI and require confirmation after showing held/upgraded/removed sets; actual update remains disabled from QA and must never auto-remove packages. For reboot capture `/proc/sys/kernel/random/boot_id`, tolerate the expected SSH transport drop after dispatch, poll bounded SSH recovery, and require a changed boot ID before success. For shutdown treat SSH unreachability only as “erişilemiyor/kapanma komutu gönderildi,” never proof of electrical power-off. Refresh health/update/security after recovery; timeouts always end in `DOĞRULANAMADI`. Must NOT execute update/reboot/poweroff or modify/invoke/test WOL during this task.
  Parallelization: Wave 4 (after Task 5, same file) | Blocked by: 2,5 | Blocks: 8
  References (executor has NO interview context - be exhaustive): `src/main.ts:1024-1156`; `src-tauri/src/lib.rs:207-232,684-705`; canonical helper actions from Task 2; Metis action-specific success matrix in draft/findings.
  Acceptance criteria (agent-executable): production build and Node built-in state-machine tests pass; each owned action has a distinct typed state machine, timeout and Turkish terminal state; update preview is read-only and actual update still requires a second explicit confirmation; fixture transport drop during reboot is not an immediate failure, unchanged boot ID never succeeds; shutdown copy avoids power-off certainty; release builds expose no fixture-injection switch or environment hook; WOL boundary hashes equal Task 1.
  QA scenarios (name the exact tool + invocation): happy: while connected, `powershell -File scripts/qa-installed-app.ps1 -Scenario ActionPrompts -CancelOnly -EvidenceDir .omo/evidence` opens update/reboot/shutdown prompts and cancels; a pure fixture verifies the preview command and confirmation content without invoking the live helper. Failure: `npx tsc --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .omo/evidence/ts-tests src/security-action-state.ts tests/security-action-state.test.ts` followed by `node --test .omo/evidence/ts-tests/tests/security-action-state.test.js` feeds same boot ID, timeout, parser error, and transport-drop sequences and requires `DOĞRULANAMADI`; release-source search proves no runtime fixture hook. The first live `--update-preview` evidence belongs to Task 7 and UI preview to Task 8. Evidence `.omo/evidence/task-6-action-prompts.png`, `task-6-preview-fixture.txt`, `task-6-timeout-state.txt`; close temporary test process.
  Commit: N | no Git repository; task evidence plus rollback manifest records the delta.

- [ ] 7. Deploy helper 1.2.2 through one direct human authorization gate and prove non-disruptive live postconditions
  What to do / Must NOT do: run `powershell -File scripts/start-supervised-deploy.ps1 -Prepare -Installer server-setup/install-jarvis-remediation.sh -Target armanc@192.168.1.10 -TimeoutSeconds 900 -EvidenceDir .omo/evidence`; it SCPs to unique `/home/armanc/JARVIS_WORKSPACE/.staging/jarvis-install-<session-id>.sh`, proves remote hash, emits session/SHA/fixed commands, and has no password parameter. Open `ssh -tt armanc@192.168.1.10`; agent types `sudo -v`, user alone types credential at visible non-echoed prompt, then agent types all non-secret commands including `sudo -k`. With cached authorization, run driver-emitted root bootstrap: a root outer shell creates `mktemp -d -p /var/tmp jarvis-deploy.XXXXXXXX` (must be `root:root` mode `0700`, meaning owner-writable but not group/world-accessible), installs source as root regular non-symlink `0700`, re-hashes, and invokes rather than `exec`s `installer.sh --supervised-deploy <id> <sha> 900`; outer shell traps exit/signals and removes only that resolved root directory after child commit/rollback. Installer validates `SUDO_USER=armanc`, captures prior metadata, installs with traps, emits ROLLBACK_ARMED, waits. Worker runs `powershell -File scripts/start-supervised-deploy.ps1 -Verify -SessionId <id> -EvidenceDir .omo/evidence` in second BatchMode connection; it returns evidence SHA. Send COMMIT only on exit 0, otherwise ABORT; missing/failed verification, timeout, or PTY disconnect rolls back. Child self-cleans transaction files before final receipt; outer root shell deletes root staging while still privileged before returning. Then agent deletes exact unprivileged staging, types `sudo -k`, verifies `sudo -n true` fails. Preserve root rollback bundle/restore command; do not exercise remediation/update/reboot/poweroff/WOL. Never weaken sudo/store credentials.
  Parallelization: Wave 5 | Blocked by: 2,3 | Blocks: 8
  References (executor has NO interview context - be exhaustive): Task 1 baseline; `server-setup/install-jarvis-remediation.sh`; server `/usr/local/sbin/jarvis-remediation`, `/etc/sudoers.d/jarvis-server-manager`; targets `armanc@192.168.1.10`, `jarvis-agent@100.105.31.70`.
  Acceptance criteria (agent-executable after the one human authentication keystroke): installed helper reports 1.2.2 and its hash matches the frozen canonical source; final helper/sudoers are regular, non-symlink, `root:root`, and exactly `0755`/`0440`; `--check`, `--status`, `--effective-config`, `--network-intel`, and `--update-preview` pass; network-intel stderr is empty and exact 3389/XRDP plus 9090/Cockpit rows have LAN/host scope; extra args and arbitrary commands are denied; `sudo -n true` remains denied after `sudo -k`; `jarvis-agent` remains no-sudo; SSH policy and pre-owned Nginx/UFW/service hashes/status remain unchanged; health has zero failures; staging paths are absent and rollback bundle remains root-owned.
  QA scenarios (name the exact tool + invocation): happy: keep the authenticated transaction supervisor PTY open with rollback armed; from a second connection run fixed helper commands only as `armanc@192.168.1.10`, then probe `armanc@192.168.1.10` and `jarvis-agent@100.105.31.70` for connectivity while limiting the latter to identity and sudo-denial checks; raw TCP probes cover 22/3389/9090. Commit through the still-open PTY only after all checks pass, clean up, `sudo -k`, and verify noninteractive sudo denial. Failure: in the Task 2 transaction harness prove HUP/timeout/external-check failure rolls back; live `sudo -n /usr/local/sbin/jarvis-remediation --update extra`, invalid rollback ID, and `sudo -n true` must fail without side effects. Evidence `.omo/evidence/task-7-server-deploy.json`, `task-7-denials.txt`, `task-7-unchanged-hashes.txt`, and `task-7-cleanup.txt`; no PTY transcript may contain credential input.
  Commit: N | server rollback bundle and task evidence are the deployment provenance.

- [ ] 8. Version, package, upgrade, and manually QA J.A.R.V.I.S v1.2.2
  What to do / Must NOT do: synchronize 1.2.2 in package/package-lock, Cargo/Cargo.lock, Tauri config, README/PROJECT; helper/installer frozen check-only, any hash change reopens Task 7. Build fresh target; checksums/unsigned disclosure. Preserve v1.2.1 installer, both app-data dirs, exact HKCU uninstall key from Task 1, settings. Reuse UI driver. Create version-independent `scripts/test-v121-recovery.ps1`: `-SandboxMatrix` works only in resolved `%TEMP%\jarvis-recovery-*`, exercising partial/corrupt/missing executable plus present/missing uninstaller but deliberately never trusting/executing that uninstaller; `-RestoreV121` closes only validated JARVIS processes, exports/removes only Task 1's exact HKCU key, moves the exact validated current install directory to timestamped recoverable quarantine regardless of current version/uninstaller state, installs saved v1.2.1 NSIS, restores both app-data dirs and preserved settings, relaunches/reconnects/verifies, and keeps quarantine until success. Run SandboxMatrix then one real RestoreV121 roundtrip on current v1.2.1 before upgrading. Install v1.2.2 NSIS, launch/connect, exercise required UI. If any upgrade gate fails, run the same version-independent RestoreV121 path; never trust current/partial uninstaller. Validate Tauri marker. Must NOT run MSI as unelevated corruption proof, remove broad paths/registry, execute disruptive actions, or invoke WOL.
  Parallelization: Wave 6 | Blocked by: 4,5,6,7 | Blocks: final verification wave
  References (executor has NO interview context - be exhaustive): `package.json`, `package-lock.json`, `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, `src-tauri/tauri.conf.json`, `helper.sh`, installer, `README.md`, `README-TR.md`, `PROJECT.md`; v1.2.1 NSIS/MSI under `src-tauri/target-install-v121`; HKCU uninstall record; v1.2.1 QA screenshots.
  Acceptance criteria (agent-executable): helper/installer hashes equal Task 7 live/frozen hashes; npm build, all tests, fresh-target Cargo check/test and Tauri release build exit 0; MSI/NSIS/EXE exist and checksum manifest verifies; all version surfaces/PE metadata/HKCU show 1.2.2; Authenticode status is recorded; SandboxMatrix covers every recovery branch and RestoreV121 proves real version-independent quarantine/reinstall/data+registry restoration on current v1.2.1 before upgrade; NSIS upgrade returns 0 and both app-data/settings snapshots remain semantically equal; one responsive installed process has expected path/title; UI shows `BAĞLI`, live Ubuntu data, correct Cockpit/XRDP scope, and Turkish failure/cancel states.
  QA scenarios (name the exact tool + invocation): before upgrade run `powershell -File scripts/test-v121-recovery.ps1 -SandboxMatrix -EvidenceDir .omo/evidence`, then `powershell -File scripts/test-v121-recovery.ps1 -RestoreV121 -EvidenceDir .omo/evidence`, both exit 0. Happy: `powershell -File scripts/qa-installed-app.ps1 -Scenario Full -Executable "$env:LOCALAPPDATA\J.A.R.V.I.S Server Manager\J.A.R.V.I.S Server Manager.exe" -Host 192.168.1.10 -Port 22 -EvidenceDir .omo/evidence` drives installed v1.2.2 and captures each surface. Failure: the same script uses a temporary invalid host, verifies bounded Turkish failure/UNKNOWN, restores original settings, reconnects, and records semantic equality; a forced SandboxMatrix partial-install/missing-uninstaller case must choose recoverable quarantine then restore. Evidence `.omo/evidence/task-8-build.txt`, `task-8-checksums.txt`, `task-8-install.log`, `task-8-connected.png`, `task-8-failure.png`, `task-8-registry.json`, and `task-8-recovery.txt`; close duplicate/temporary processes and leave one app instance only if requested.
  Commit: N | no Git repository; final archive/checksum/evidence set is the release record.

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE; completion is evidence-driven and needs no second user approval beyond Task 7's direct authorization gate.
- [ ] F1. Plan compliance audit — launch `lazycodex-gate-reviewer` as `f1_plan_compliance` with the final plan SHA, baseline manifest, ledger, and Tasks 1-8 evidence paths; it must map every Must have/Must NOT have and every acceptance clause to an exact current file, command output, and hash-pinned artifact. Root records its untouched terminal report plus agent ID/SHA in `.omo/evidence/final/f1-plan-compliance.md` and the ledger. Expected pass: `APPROVE`, zero missing/stale/self-reported-only mappings; any gap is `REJECT` and reopens its owning task.
- [ ] F2. Code quality and security review — launch `lazycodex-code-reviewer` as `f2_code_security` with baseline/final manifests and source SHA set; audit complete delta for injection, symlink/TOCTOU, sudo argv, transaction rollback, protocol, stale success, secrets, release fixture absence, maintainability; independently rerun gates. Root records untouched report, agent ID, final plan/release SHA, verdict in `.omo/evidence/final/f2-code-security.md` and ledger. Expected `APPROVE`, all exits 0, no unresolved security finding; otherwise reopen owner.
- [ ] F3. Real manual QA — launch `lazycodex-qa-executor` as `f3_installed_manual_qa`; run `powershell -File scripts/qa-installed-app.ps1 -Scenario Full -Executable "$env:LOCALAPPDATA\J.A.R.V.I.S Server Manager\J.A.R.V.I.S Server Manager.exe" -Host 192.168.1.10 -Port 22 -EvidenceDir .omo/evidence/final/f3`, connect installed v1.2.2, validate Dashboard/Files/Security/update-preview/reboot-shutdown cancel paths, capture foreground screenshots, rerun server health/hash checks, and never invoke remote-open/WOL or disruptive actions. Root records the untouched report, agent ID, final plan SHA, release-manifest SHA, and verdict in `.omo/evidence/final/f3-manual-qa.md` and the ledger. Expected pass: `APPROVE`, all scripted assertions pass, fresh screenshots exist, and server hashes/health match; otherwise reopen the owner.
- [ ] F4. Scope fidelity — launch `lazycodex-gate-reviewer` as `f4_scope_fidelity` with Task 1/final manifests/forbidden list; rerun WOL extractor, diff Nginx/UFW/SSH/service/package/Git/GitHub/signing state, inspect logs. Root records untouched report, agent ID, final plan/release SHA, verdict in `.omo/evidence/final/f4-scope-fidelity.md` and ledger. Expected `APPROVE`, every frozen hash identical, no forbidden action, residuals disclosed; mismatch reopens owner.

## Commit strategy
- Direct-work mode because the source is not a Git repository. Do not initialize Git silently.
- Task 1 produces a full prechange source/installed/server metadata rollback archive and hash manifest; every later task appends changed-file hashes and QA evidence.
- Task 7 preserves exact helper/sudoers bytes, presence, owner and mode before the per-file transactional deployment and owns the server restore receipt.
- Task 8 preserves v1.2.1 NSIS installer/settings and produces the v1.2.2 archive/checksum/install evidence.

## Success criteria
- Every top-level Task 1-8 and F1-F4 checkbox is complete with a confirmed independent verdict and redacted artifact.
- Current-source helpers are synchronized at 1.2.2; live helper hash/version match; fixed/malformed sudo actions behave exactly; general passwordless sudo remains denied.
- Live `--network-intel` has zero warning stderr and names Cockpit/XRDP with observable host-firewall source scope; no false WAN claim.
- SSH effective policy, LAN SSH, Tailscale SSH, UFW/Nginx hashes/status and health are unchanged except the explicitly owned helper/sudoers files.
- File-manager symlink traversal attempts fail visibly while normal in-home files work; the threat-model limitation is documented.
- Installed per-user v1.2.2 opens, connects, presents fully Turkish actionable states, and never claims apply/rollback/update/power success without the specified postcondition; remote-open/WOL remains untouched and untested.
- Fresh NSIS/MSI/EXE artifacts and checksum manifest verify; unsigned status and MSI privilege model are disclosed; v1.2.1 recovery path remains usable.
- No secret appears in source, evidence, logs, plan, or ledger; no disruptive live operation was performed.
